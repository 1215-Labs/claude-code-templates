name: [Must be unique, descriptive]
description: [2-3 sentences on what agent does]
model: [sonnet or haiku, sonnet recommended]
```

---

### **3. COMMANDS DIRECTORY** (`/.claude/commands/`)
Commands are quick shortcuts for common operations (not heavily used in this repo, but available).

**Structure**:
```
.claude/
└── commands/
    ├── command-name.md
    ├── another-command.md
    └── [additional-commands.md]